{% extends 'base.html.twig' %}
{% block title %}Editar Torneo {% endblock title %}
{% block body %}
    <h2>Editar Torneo</h2>
            <hr>
            {{ form_start(formulario, {'attr': {'id': 'formTorneo'}}) }}
            {{ form_end(formulario) }}
            <a href="{{path()}}" data-torneo-id="{{ torneo.id }}" id="btn-add-equipo">Añadir equipo</a>
            <a href="{{ path('torneo',{'id': torneo.id}) }}">Cancelar</a>
            <div id="equipos"></div>
{% endblock %}